modules:
  - name: chromium
    install:
      - pacman:chromium
      - apt:chromium

  - name: firefox
    install:
      - pacman:firefox
      - apt:firefox
    exec: |
      if check_cmd xdg-settings; then
        xdg-settings set default-web-browser firefox.desktop
      fi
