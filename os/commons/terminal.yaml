modules:
  - name: tmux
    install:
      - pacman:tmux
      - apt:tmux
    config: ~/.config/tmux

  - name: ghostty
    install:
      - pacman:ghostty
    config: ~/.config/ghostty

  - name: alacritty
    install:
      - pacman:alacritty
      - apt:alacritty
    config: ~/.config/alacritty
